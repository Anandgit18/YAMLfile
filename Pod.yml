---
api Version: v1
kind: Pod
metadata:
    name: busapp-pod
    labels:
       app: frontend
spec: 
    containers: 
       - name: busapp-container
         image: busap
         ports:
          -name: busapp-port
           containerPort: 3000
